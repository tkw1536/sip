import { h, Component } from 'preact';
import type { ViewProps } from "../viewer";

export default class GraphView extends Component<ViewProps> {
    render() {
        return <code>GraphView Unimplemented</code>
    }
}